(library
  (name data_flow)
  (modules data_flow)
  (libraries control_flow))

(library
  (name dominators)
  (modules dominators)
  (libraries data_flow))

(library
  (name liveness)
  (modules liveness)
  (libraries data_flow))
