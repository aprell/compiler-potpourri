(library
  (name basic_block)
  (public_name basic_block)
  (modules (:standard \ test_basic_block))
  (libraries three_address_code))

(executable
  (name test_basic_block)
  (modules test_basic_block)
  (libraries basic_block))
