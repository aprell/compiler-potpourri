(library
  (name control_flow)
  (public_name control_flow)
  (modules cfg dom loop graphs utils)
  (libraries basic_block str))

(executable
  (name test_control_flow)
  (modules test_control_flow)
  (libraries control_flow))
