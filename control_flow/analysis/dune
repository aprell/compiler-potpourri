(library
  (name control_flow)
  (public_name control_flow)
  (modules cfg dom inspect)
  (libraries basic))

(executable
  (name test_cfg)
  (modules test_cfg)
  (libraries control_flow))

(executable
  (name test_dom)
  (modules test_dom)
  (libraries control_flow))
